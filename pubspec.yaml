name: note_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator:  ^1.1.3
  build_runner: ^2.1.11

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true